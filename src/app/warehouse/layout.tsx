import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Warehouse | MultiPass Studio', description: '23 fiches knowledge, pipeline 5 agents, indexation Mem0, 15 categories.' }
export default function Layout({ children }: { children: React.ReactNode }) { return children }
