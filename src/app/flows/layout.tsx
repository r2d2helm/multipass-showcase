import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Flows | MultiPass Studio', description: 'Workflows interactifs: dev cycle, warehouse pipeline, incident recovery.' }
export default function Layout({ children }: { children: React.ReactNode }) { return children }
