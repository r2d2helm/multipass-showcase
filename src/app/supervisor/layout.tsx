import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'MegaSupervisor | MultiPass Studio', description: 'Recovery L1-L5, reconstruction ecosysteme, 16 probes externes, incident playbooks.' }
export default function Layout({ children }: { children: React.ReactNode }) { return children }
