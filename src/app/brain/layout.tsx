import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Brain | MultiPass Studio', description: 'PAI Algorithm v1.8.0, 13 agents IA, 28 subagents, 15 skills, 11 hooks.' }
export default function Layout({ children }: { children: React.ReactNode }) { return children }
