import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Security | MultiPass Studio', description: '6 couches defense, 31 controles securite, zero ports ouverts, threat model.' }
export default function Layout({ children }: { children: React.ReactNode }) { return children }
