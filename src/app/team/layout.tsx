import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Team | MultiPass Studio', description: 'L\'equipe MultiPass: 1 human, 1 AI, 13 agents, 28 subagents, 3 services core.' }
export default function Layout({ children }: { children: React.ReactNode }) { return children }
