import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Franchise | MultiPass Studio', description: 'Pack commercial franchise MultiPass: 3 tiers, ecosysteme IA complet, comparatif.' }
export default function Layout({ children }: { children: React.ReactNode }) { return children }
