import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Roadmap | MultiPass Studio', description: 'Developpement en cours: FarmSysteme, Agency v2.0, PAI v4.0, Knowledge Platform, et plus.' }
export default function Layout({ children }: { children: React.ReactNode }) { return children }
