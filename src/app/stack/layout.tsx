import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Stack | MultiPass Studio', description: '30 containers Docker, 7 stacks Compose, architecture interactive des services.' }
export default function Layout({ children }: { children: React.ReactNode }) { return children }
