import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Portfolio | MultiPass Studio', description: '7 projets types web et mobile: restauration, immobilier, sante, e-commerce, fitness, logistique, education.' }
export default function Layout({ children }: { children: React.ReactNode }) { return children }
